<Window x:Class="MathGame.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MathGame"
        mc:Ignorable="d"
        Title="Linneas mattespel" Height="100" Width="300">
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <DockPanel Grid.Column="0" DockPanel.Dock="Top">
            <Label DockPanel.Dock="Left" VerticalAlignment="Center" FontSize="18">Problem:</Label>
            <TextBlock VerticalAlignment="Center" FontSize="18" Text="{Binding Path=Problem.Value}" />
        </DockPanel>
        <DockPanel Grid.Column="1" DockPanel.Dock="Top">
            <Label DockPanel.Dock="Left" FontSize="18" VerticalAlignment="Center">Lösning:</Label>
            <TextBox VerticalAlignment="Center" FontSize="18" Text="{Binding Path=Solution.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" KeyDown="OnKeyDownHandler" PreviewTextInput="PreviewTextInput"/>
        </DockPanel>
    </Grid>
</Window>
